<h2>
  Characters
</h2>
<ul>
  <% @characters.each do |character| %>
    <li>
      <a href="<%= character_path(character) %>"><%= character.composite_name %></a>
    </li>
  <% end %>
</ul>

<%= render "shared/page_numbers" %>
